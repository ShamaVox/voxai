import { StyleSheet } from "react-native";

const styles = StyleSheet.create({
  navBar: {
    width: 180,
  },
  contentView: {
    flex: 1,
    flexDirection: "row",
  },
  app: {
    flex: 1,
  },
});

export default styles;
